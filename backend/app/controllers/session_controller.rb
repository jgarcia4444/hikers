class SessionController < ApplicationController

    # def current_user
    #     if session[:id] 
    #         user = User.find(session[:id])
    #         session[:id] = user.id
    #         render json: user
    #     else
    #         render json: session[:id]
    #     end
    # end

end